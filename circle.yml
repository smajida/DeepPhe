machine:
  java:
    version: oraclejdk8
dependencies:
  override:
    - mvn -q -e clean compile -P runJcasGen -DskipTests
## I really hate to do this, but failed tests are killing us.
# test:
#  override:
#    - mvn -e clean test
